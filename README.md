# 《阳光竞技场》项目文档与游戏规则指南

欢迎查阅《阳光竞技场》（Sunny Arena）项目技术与玩法文档。本项目是一款基于现代前端架构开发的**卡通扁平风（Warm Cartoon Flat Design）回合制对决网页游戏**。

---

## 目录
1. [项目概述与技术细节](#一项目概述与技术细节)
2. [项目目录结构](#二项目目录结构)
3. [核心游戏规则与战斗机制](#三核心游戏规则与战斗机制)
4. [魔物预设与图鉴说明](#四魔物预设与图鉴说明)
5. [游戏参数调整与配置指南](#五游戏参数调整与配置指南)
   - [5.1 在游戏界面直接调整（免改代码）](#51-在游戏界面直接调整免改代码)
   - [5.2 在代码中修改默认参数与数值平衡](#52-在代码中修改默认参数与数值平衡)
   - [5.3 新增自定义 BOSS 魔物](#53-新增自定义-boss-魔物)
6. [开发与构建指引](#六开发与构建指引)

---

## 一、项目概述与技术细节

- **技术栈**：
  - **核心框架**：React 18 + Vite + TypeScript
  - **样式与排版**：Tailwind CSS（采用温暖马卡龙/米黄柔和基底 `#FFE9C7` ~ `#FFD3A3`，全界面圆角微质感，针对手机端与桌面端全尺寸适配）
  - **动效引擎**：`motion/react`（Framer Motion）（负责卡牌翻转、摇晃蓄力、冲刺撞击打击感、震屏、伤害漂浮与弹窗平滑过渡）
  - **图标系统**：`lucide-react`
  - **音效系统**：基于 Web Audio API 纯原生合成音频（无外部音频资源依赖，内置击中、冲刺、治疗、胜利、失败等实时波形合成）
- **核心特色**：
  - **盲盒悬念**：BOSS 初始生命值与攻击力在战斗开始前完全隐藏。
  - **半血战况揭秘**：血量降至临界线时触发侦察情报，以环形仪表盘呈现双方实时百分比。
  - **防扰动时序保护**：回合严格依照“玩家行动 ➔ BOSS 反击 ➔ 回合结算 ➔ 检查警报弹窗”的时序执行，杜绝动画冲突与弹窗闪烁。

---

## 二、项目目录结构

```text
├── index.html                    # 单页入口 HTML（包含视口配置与字体载入）
├── metadata.json                 # 应用元数据（名称、描述及平台能力声明）
├── package.json                  # 依赖与启动脚本
├── tsconfig.json                 # TypeScript 编译配置
├── vite.config.ts                # Vite 构建配置
├── README.md                     # 项目文档（本文件）
└── src/
    ├── main.tsx                  # React 挂载入口
    ├── App.tsx                   # 根组件与页面路由状态调度
    ├── index.css                 # 全局 Tailwind 样式定义与手机端防误缩放
    ├── types.ts                  # 全局 TypeScript 接口与类型定义
    ├── constants.ts              # 默认游戏平衡参数与 BOSS 预设数据表
    ├── sound.ts                  # Web Audio API 纯代码合成音效引擎
    ├── components/
    │   ├── battle/
    │   │   ├── Arena.tsx         # 战斗舞台（角色站位、攻击对冲动画与震屏反馈）
    │   │   ├── DamageFloat.tsx   # 伤害飘字组件（BOSS飘红、玩家飘蓝、治疗飘绿）
    │   │   └── RevealPanel.tsx   # 半血战况警报弹窗（双仪表盘对比与战术选择）
    │   └── common/
    │       ├── Avatar.tsx        # 玩家与魔物矢量/Emoji卡通角色头像底座
    │       ├── Button.tsx        # 卡通立体质感操作按钮
    │       └── HealthBar.tsx     # 动态血条（平滑缓动、濒危脉冲、数字/百分比切换）
    └── pages/
        ├── SetupPage.tsx         # 勇士数值配置与高级规则面板
        ├── DraftPage.tsx         # 魔物盲盒抽取与情报展示页
        ├── BattlePage.tsx        # 核心战斗回合流转页面
        └── ResultPage.tsx        # 战后数据清单与结算回顾页
```

---

## 三、核心游戏规则与战斗机制

### 3.1 游戏流程阶段
1. **属性配置（Setup）**：设定勇士初始生命值、攻击力，也可展开高级选项自定义补给、浮动比例等；
2. **抽取魔物（Draft）**：点击盲盒卡牌抽取对手（分为低阶、中阶、高阶三种难度，属性暂为秘密）；
3. **回合对决（Battle）**：进入战斗舞台，展开回合交锋；
4. **战局结算（Result）**：击败魔物、不幸阵亡或中途撤退，汇总展示交锋回合、总输出/承受伤害及补给消耗。

### 3.2 回合交锋时序（Turn Order）
- 玩家每回合拥有优先选择权，可在以下三项行动中选择一项：
  1. **普通攻击**：勇士蓄力冲刺，对 BOSS 造成等同于玩家攻击力的直接伤害；
  2. **使用补给**：消耗 1 份补给包，为勇士恢复指定生命值（不超过上限）；
  3. **战略撤退**：脱离战斗，立即进入战果结算页面。
- **BOSS 反击逻辑**：玩家行动完成后（无论攻击还是补给），若 BOSS 未死亡，BOSS 会必定发起反击冲刺。
- **伤害公式**：
  - 玩家对 BOSS 伤害：$Damage_{Player} = Attack_{Player}$
  - BOSS 对玩家伤害：$Damage_{BOSS} = \text{round}(Attack_{BOSS} \times (1 + \Delta))$，其中 $\Delta \in [-\text{fluctuation}, +\text{fluctuation}]$
- **受击色彩规范**：
  - BOSS 受击：红色飘字（`-XX`）与暖红震屏；
  - 玩家受击：蓝色飘字（`-XX`）与受击微震；
  - 生命恢复：绿色飘字（`+XX`）与光环特效。

### 3.3 半血战况揭示机制（Half-HP Reveal）
- **触发条件**：当玩家或 BOSS 的当前生命值首次降至总生命的 **50%（或自定义阈值）** 以下时触发。
- **时序保护（避免闪烁）**：
  - 若玩家攻击导致 BOSS 破半血，**不会在此瞬间立刻打断界面**，而是先让 BOSS 正常完成本回合的反击冲刺；
  - 待整回合双方伤害全部结算后，再向玩家弹出“战况警报·半血揭示”弹窗；
  - 弹窗内可直观查看双方当前百分比，并供玩家选择“继续攻击”、“使用补给”或“果断撤退”；
  - 弹窗操作具备防连击锁定（`isBusy` 保护），避免快速连续点击导致状态异常。
- **侦察次数上限**：最多进行 $1 + \text{maxExtraReveals}$ 次侦察提示（默认最多 3 次）。

---

## 四、魔物预设与图鉴说明

系统内置 3 档经典卡通魔物，预设定义在 `/src/constants.ts` 中：

| 档位 | 魔物名称 | 称号 | 基础生命 | 基础攻击 | 外观 Emoji | 难度特点 |
| :--- | :--- | :--- | :---: | :---: | :---: | :--- |
| **低阶（Low）** | 小石怪·布布 | 普通的森林巡逻者 | 80 | 8 | 🪨 | 呆萌笨拙，适合熟悉规则的新手 |
| **中阶（Mid）** | 暗夜狼王·芬里尔 | 嗜血的雪原猎手 | 120 | 15 | 🐺 | 攻击力显著提升，需合理运用补给 |
| **高阶（High）** | 烈焰炎魔·拉格纳 | 深渊的烈焰领主 | 180 | 25 | 🔥 | 高血高攻强敌，极度考验数值配比 |

---

## 五、游戏参数调整与配置指南

### 5.1 在游戏界面直接调整（免改代码）

在游戏首屏（勇士属性设定页），提供两层配置入口：

1. **基础设置**：
   - **生命值（HP）**：支持滑动条与快捷步进按钮调整（范围：`50 ~ 300`，步长：`10`）。
   - **攻击力（ATK）**：支持滑动条与快捷步进按钮调整（范围：`5 ~ 60`，步长：`5`）。
2. **高级规则面板（点击展开）**：
   - **BOSS 攻击浮动率（Fluctuation）**：默认 `10%`（即实际攻击在 `90% ~ 110%` 随机波动，可在 `0% ~ 30%` 调整）。
   - **携带补给数量（Max Supplies）**：默认 `3` 个（可选 `0 ~ 5` 个）。
   - **半血警报阈值（Threshold）**：默认 `50%`（可选 `30% ~ 60%`）。
   - **战况侦察次数（Max Reveals）**：额外侦察次数，默认 `2` 次（总计最多 3 次）。

---

### 5.2 在代码中修改默认参数与数值平衡

若想调整每次刷新页面后的**默认数值**或拓展限制，请编辑文件：
👉 **`/src/constants.ts`**

```typescript
export const DEFAULT_CONFIG: GameConfig = {
  playerHP: 100,            // 玩家默认初始生命值
  playerAttack: 20,         // 玩家默认攻击力
  bossFluctuation: 0.1,     // BOSS 攻击浮动率（0.1 = +/-10%）
  maxSupplies: 3,           // 默认携带补给包数量
  supplyHealAmount: 35,     // 每次使用补给包恢复的生命值点数
  halfHpThreshold: 0.5,     // 半血揭晓触发比例（0.5 = 50%）
  maxExtraReveals: 2,       // 额外侦察提示次数（1次初始 + 2次额外 = 共3次）
};
```

---

### 5.3 新增自定义 BOSS 魔物

在 `/src/constants.ts` 中的 `BOSS_PRESETS` 对象中，可以直接增加或修改现有魔物：

```typescript
export const BOSS_PRESETS: Record<string, BossPreset> = {
  // 可以修改现有魔物数值，或仿照以下结构调整：
  low: {
    id: 'low',
    name: '小石怪·布布',
    title: '普通的森林巡逻者',
    tier: 'low',
    tierLabel: '低阶魔物',
    maxHP: 80,             // 基础生命上限
    attack: 8,             // 基础攻击力
    avatar: '🪨',          // 显示的卡通 Emoji 或图标
    desc: '看起来呆萌笨拙，动作迟缓，但有一颗坚固的心。',
    themeColor: '#8A9A5B',
  },
  // ... 其他 BOSS 预设
};
```

---

## 六、开发与构建指引

### 运行环境
- **Node.js**: 18.0 或更高版本
- **包管理器**: npm

### 常用命令
```bash
# 安装依赖
npm install

# 启动本地开发服务（运行在 http://localhost:3000）
npm run dev

# 语法与类型校验
npm run lint

# 生产环境打包构建
npm run build
```

---
*文档版本：v1.0.0 ｜ 项目遵循现代卡通扁平化交互与无障碍排版设计。*
